services:
  - type: web
    name: zentrix-motors-api
    env: node
    plan: free
    buildCommand: rm -rf node_modules && npm install && npm rebuild sqlite3
    startCommand: node server.js
    rootDir: backend
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
